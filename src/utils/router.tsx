// import React from "react";
// import {
//   BrowserRouter as ReactRouterBrowserRouter,
//   BrowserRouterProps,
// } from "react-router-dom";

// interface BrowserRouterOptions extends BrowserRouterProps {
//   basename?: string;
// }

// export function createBrowserRouter({
//   basename,
//   ...options
// }: BrowserRouterOptions = {}) {
//   const BrowserRouter = ({ children }: { children: React.ReactNode }) => (
//     <ReactRouterBrowserRouter basename={basename} {...options}>
//       {children}
//     </ReactRouterBrowserRouter>
//   );

//   return BrowserRouter;
// }
